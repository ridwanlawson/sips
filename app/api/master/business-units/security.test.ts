import { describe, it, expect, vi, beforeEach } from 'vitest';import { GET } from './route';import { NextRequest } from 'next/server';vi.mock('@/utils/api/absensiProxy', () => ({  BACKEND_URL: 'http://backend.test',}));describe('Business Units API Security', () => {  beforeEach(() => {    vi.stubGlobal('fetch', vi.fn());    vi.clearAllMocks();  });  it('should not leak upstream error message on failure (CWE-209)', async () => {    const mockRequest = new NextRequest('http://localhost/api/business-units', {      headers: { cookie: 'auth_token=valid-token' },    });    const sensitiveError = 'Internal Database Error: Connection failed at line 42';    vi.mocked(fetch).mockResolvedValueOnce({      ok: false,      status: 500,      text: async () => sensitiveError,    } as Response);    const response = await GET(mockRequest);    const body = await response.json();    expect(response.status).toBe(500);    expect(body.ok).toBe(false);    expect(body.error).toBe('Failed to fetch business units');    expect(body.error).not.toContain(sensitiveError);  });});